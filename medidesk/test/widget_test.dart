// Smoke test — verifies MediDeskApp mounts without crashing.
// Full integration tests require a running build_runner pass first.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — run build_runner before widget tests', () {
    // After running:
    //   flutter pub run build_runner build --delete-conflicting-outputs
    // replace this with real widget/integration tests.
    expect(true, isTrue);
  });
}
