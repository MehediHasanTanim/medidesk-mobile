import 'package:flutter/material.dart';
class ConsultationFormScreen extends StatelessWidget {
  const ConsultationFormScreen({super.key, required this.consultationId});
  final String consultationId;
  @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Consultation Form')), body: const Center(child: Text('TODO')));
}
