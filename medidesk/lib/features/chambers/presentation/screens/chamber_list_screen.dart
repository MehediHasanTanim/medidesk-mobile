import 'package:flutter/material.dart';

class ChamberListScreen extends StatelessWidget {
  const ChamberListScreen({super.key});
  @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Chambers')));
}
